{
  "name": "lulu-new",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend start",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start"
  }
}
